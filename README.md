# Coisas desenvolvidas na aula de programacão para dispostivos móveis

nada muito especial, somente códigos muito ruins estarão aqui, por exemplo:
```python
functools.cache
def fatRec(a:int) -> int:
    if a <= 1:
        return 1
    else:
        return a * fatRec(a-1)
```
